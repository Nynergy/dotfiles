# [urxvt] Add version info to Xresources

- Created:  June 09, 2026
- Status:   CLOSED

Now that colorschemes are no longer just `head -n` data ripped from the
.Xresources file, we can actually include some version info in the header.
