# misc

It's where all the other stuff goes.

-------------------------------------------------------------------------------

## Contents

- `colorschemes` :: A directory of terminal colorschemes in .Xresources format.
- `layouts` :: A directory of screen layouts in xrandr format (sort of).
